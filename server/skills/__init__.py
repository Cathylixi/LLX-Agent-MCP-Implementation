"""
The skills library.

Each .py file in this folder is one skill. To add a skill, drop a new file
here that imports `mcp` from app and defines a function decorated with
@mcp.tool(). main.py auto-discovers and loads every file in this folder.
"""
